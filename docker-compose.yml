version: '3.8'

services:
  exam_app:
    build: .
    container_name: exam_app
    ports:
      - "8010:8000"
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - CLAUDE_API_KEY=${CLAUDE_API_KEY}
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    user: "0:0"